$("#ajaxContainer").html("<%= escape_javascript(render 'listings') %>")
//Rails first looks for a render path inside users/registrations. If it can't find anything, it goes out to views , hence the properties/listings
